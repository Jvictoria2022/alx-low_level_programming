#include <stdio.h>
/**
 * main - prints all single digit numbers of base 10 starting from 0
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int y;

	for (y = 0; y < 10 ; y++)
	putchar(y);
	putchar('\n');
	return (0);
}
