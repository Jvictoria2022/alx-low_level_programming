#include "main.h"
/**
 * _abs - Entry point
 *@t: computes the absolute value of an integer
 *
 *Return: 1 if c is  is a letter, lowercase or uppercase,otherwise 0
 */
int _abs(int t)
{
	if (n >= 0)
	return (n);
	else
		return (-n);
}
